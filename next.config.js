/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Configuración compatible con Vercel, Railway y Render
  // No necesita output: 'standalone' para estas plataformas
}

module.exports = nextConfig

